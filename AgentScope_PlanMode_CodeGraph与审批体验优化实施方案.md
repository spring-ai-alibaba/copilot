# AgentScope Plan Mode：CodeGraph 与审批体验优化实施方案

## 1. 背景与目标

当前 Plan Mode 已具备只读规划、风险分级、用户审批、Todo 执行和状态恢复能力，但审批卡的主体仍偏向原始技术 Markdown。对普通用户而言，难以快速回答三个关键问题：**最终会得到什么、具体会改什么、如何确认改对了。**

同时，纯 `rg + read_file` 更擅长文本定位，不足以稳定回答调用链、影响范围和关联测试。项目已经具备可用的 CodeGraph 索引，因此应将其作为编码规划阶段的代码理解底座。

本方案的目标：

- 将审批计划变为“先结论、后细节”的可读决策界面。
- 以 CodeGraph 为计划提供符号、调用链、影响范围和测试证据。
- 保持 Plan Mode 的只读、安全和可回退特性。
- 让执行计划、Todo 和最终验证形成可审计的完整闭环。

## 2. 用户体验方案

### 2.1 审批抽屉的默认结构

计划继续位于输入框上方的上拉抽屉中；默认展示决策摘要，而不是完整 `PLAN.md`。

| 区块 | 面向用户的内容 | 默认状态 |
| --- | --- | --- |
| 你将得到什么 | 一句话描述最终交付与用户价值 | 展开 |
| 计划怎么做 | 3–6 个自然语言步骤：改哪里、为什么改、预期结果 | 展开 |
| 影响范围 | 修改文件/符号、核心调用链、不修改的范围 | 展开 |
| 如何验证 | 自动化测试、构建检查、必要的手动验证及预期结果 | 展开 |
| 风险与边界 | 兼容性、权限、数据或不可逆风险与缓解方式 | 有风险时展开 |
| 需要你决定 | 阻塞性问题、建议选项、默认建议 | 有问题时展开 |
| 技术详情 | 原始计划、Git 状态、文件预览、完整证据 | 默认收起 |

审批区固定在抽屉底部：`批准并执行`、`要求修改`。如果存在阻塞性选择，批准按钮应提示先完成选择；中高风险计划应清晰说明需要确认的原因。

### 2.2 计划步骤卡片

每个步骤采用统一的可读卡片，而不是段落堆叠：

```text
步骤 2：统一登录失败提示
改动：LoginForm.tsx、AuthService.authenticate
原因：前端当前无法区分凭据错误和网络故障
影响：登录表单 → AuthController → AuthService
完成标准：错误密码会显示明确提示，网络异常保留重试入口
验证：pnpm test；mvn test -pl copilot-context
```

文件和符号应支持跳转/复制；调用链只显示 1–2 层关键路径，完整链路放入“技术详情”，避免卡片信息过载。

### 2.3 执行后的反馈

- 批准后：抽屉收起，首批 Todo 到达时自动展开。
- 执行中：展示任务进度、当前正在验证的事项和可展开的执行证据。
- 完成后：自动收起，仅保留完成比例；用户可再次展开回看。
- 失败或计划偏离：自动展开，说明失败原因、已完成内容、下一步建议和是否需要重新审批。

## 3. CodeGraph 接入设计

### 3.1 定位

CodeGraph 是规划阶段的**代码理解器**，不替代 Agent 的推理、用户沟通或执行能力。

```mermaid
flowchart LR
    U[用户需求] --> P[Plan Agent]
    P --> G[CodeGraph 受控查询]
    G --> E[符号、调用链、影响面、测试证据]
    E --> R[结构化审批计划]
    R --> A[用户批准]
    A --> X[Agent 执行与验证]
```

### 3.2 Agent 可调用的只读工具

不向模型开放任意 Shell；后端以固定参数封装以下工具：

| 工具 | 用途 | 主要输出 |
| --- | --- | --- |
| `codegraph_search` | 按功能或符号定位代码 | 文件、符号、摘要 |
| `codegraph_explore` | 读取目标符号及上下文 | 关键源码、关联节点 |
| `codegraph_impact` | 分析改动影响面 | 上游调用者、下游依赖、受影响模块 |
| `codegraph_affected_tests` | 定位关联测试和验证入口 | 测试文件、测试命令建议 |

每个工具必须满足：

- 仓库根目录由服务端解析，不能由模型或用户传入任意绝对路径。
- 子命令、参数白名单、超时时间和最大输出大小均由服务端固定。
- 返回结构化 JSON，而不是未经限制的命令行输出。
- 只允许查询；索引初始化和同步由平台后台任务负责，不能由模型触发写操作。
- CodeGraph 不可用、索引过期或语言不支持时，降级至现有 `rg + read_file + git`，并在计划中标注证据来源。

### 3.3 索引生命周期

当前会话工作区按会话隔离，不能简单地让每个会话重复建立索引。应以“仓库身份 + 提交/文件快照”管理索引：

1. 平台检测仓库是否已索引且是否过期。
2. 缺失或过期时，由受控后台任务建立或同步索引。
3. 规划 Agent 只读取可用索引；未就绪时采用降级方案，不阻塞普通计划。
4. 前端在技术详情中显示证据来源与索引更新时间，避免用户误以为链路来自实时执行。

## 4. 结构化数据契约

扩展现有 `PlanWorkspaceDTO.PlanReview`，保留原字段以兼容历史会话，并新增以下可选数据：

```json
{
  "summary": "新增登录失败提示并补齐鉴权测试",
  "changes": [
    {
      "title": "统一登录失败提示",
      "files": ["ui-react/src/.../LoginForm.tsx"],
      "symbols": ["LoginForm", "AuthService.authenticate"],
      "reason": "区分凭据错误和网络故障",
      "acceptanceCriteria": ["错误密码显示明确提示"],
      "verification": ["pnpm test", "mvn test -pl copilot-context"],
      "evidence": ["LoginForm → AuthController → AuthService"]
    }
  ],
  "scopeOut": ["不修改数据库结构"],
  "risks": [],
  "questions": [],
  "technicalPlan": "原始 PLAN.md 内容"
}
```

Todo 同步扩展为：`acceptanceCriteria`、`verificationCommand`、`verificationStatus`、`evidence` 与 `blockedReason`。状态建议统一为：`pending`、`in_progress`、`verifying`、`completed`、`blocked`、`failed`。

## 5. 编码计划质量门槛

计划进入审批前，后端执行质量校验：

- 至少包含真实文件路径或目标符号，不能只有泛泛描述。
- 每个改动步骤都必须给出完成标准。
- 至少有一项自动化或手动验证方式，并写明预期结果。
- 明确变更边界与不变更范围。
- 中高风险操作必须有风险说明与回退思路。
- CodeGraph 可用时，关键改动至少有一项调用链、影响面或关联测试证据。
- 技术栈由仓库实际情况识别；禁止再使用“Java + Vue”等硬编码模板假设。

如果执行中发现计划外文件、依赖升级、权限/数据风险提升或核心方案变更，Agent 应暂停，生成“计划变更说明”，并重新请求批准。

## 6. 实施分期

### 阶段一：审批计划信息架构与界面（1–2 天）

- 后端把现有计划解析为摘要、步骤、范围、验证和风险结构。
- 前端将审批抽屉改为决策摘要 + 折叠技术详情。
- 保持对旧 `arc-plan` 与历史 `PLAN.md` 的兼容回退。
- 更新成果介绍与演示手册。

**验收：** 用户不打开技术详情，也能回答“会改什么、为什么、如何验证、有什么风险”。

### 阶段二：CodeGraph 受控适配（2–3 天）

- 实现 CodeGraph 服务适配器、命令白名单、超时/输出限制和结构化解析。
- 将四个只读工具注册到 Plan Agent。
- 引入索引状态检查、后台同步策略和 `rg + read_file + git` 降级路径。
- 将调用链、影响面、关联测试写入计划证据。

**验收：** 对一个跨前后端改动，审批卡能展示真实的受影响符号/链路，并能在 CodeGraph 停用时正常生成计划。

### 阶段三：执行契约与重新审批（1–2 天）

- Todo 增加验收标准、验证状态和执行证据。
- 在计划偏离时生成差异摘要，并阻止未批准的风险扩大。
- 最终回复输出改动摘要、验证结果、未完成项与风险提示。

**验收：** 执行完成后能从面板回溯每个 Todo 的完成标准和验证证据；计划偏离时不会静默继续。

### 阶段四：测试与演示（1 天）

- 后端：解析、质量门槛、工具安全、降级、会话隔离、计划状态流转测试。
- 前端：摘要渲染、折叠、阻塞问题、恢复、错误状态与移动窗口布局测试。
- 手工演示：普通小改动、跨模块改动、CodeGraph 不可用、计划偏离四条路径。

## 7. 风险与决策

| 风险 | 应对 |
| --- | --- |
| CodeGraph CLI 在部署环境缺失 | 启动健康检查；不可用时自动降级，不影响普通规划 |
| 每会话重复索引导致性能差 | 按仓库身份缓存索引，由后台统一同步 |
| 计划内容过长 | 默认摘要优先，完整 Markdown 与证据折叠 |
| 模型滥用查询能力 | 服务端固定命令/路径/参数，禁止通用 Shell |
| 索引与工作区内容不一致 | 显示索引时间；必要时标记证据可能过期并降级 |

## 8. 推荐推进顺序

先实施**阶段一**。它不依赖部署 CodeGraph，能立刻改善用户审批体验；完成后再实施阶段二，让每份编码计划拥有可解释的代码链路证据。阶段三最后接入，使“计划—执行—验证”成为完整闭环。

## 9. 当前实现进度（2026-08-05）

- 已完成阶段一：后端将 `PLAN.md` 自动提取为交付摘要、变更步骤、影响范围、验证项、风险和待确认问题；输入框上方抽屉优先展示这些决策信息，原始计划与现场信息收纳在“技术详情”。历史会话缺失结构化字段时仍回退展示原始计划。
- 已完成阶段二的受控查询入口：`codegraph_search`、`codegraph_explore`、`codegraph_impact` 与 `codegraph_affected_tests` 只会在已存在 `.codegraph` 索引的会话工作区注册；服务端固定 CLI 子命令、项目根目录、超时和输出上限，禁止 Agent 调用 `init`、`sync` 或任意 Shell。
- 已接入第一类自动证据：计划生成后会针对最多 3 个影响文件查询 CodeGraph 的关联测试，并在审批抽屉的“代码证据”区展示。查询不可用或无结果时静默省略，不影响生成与审批。
- 已配置降级：工作区未索引、禁用或 CLI 不可用时，Agent 继续使用既有的 `read_file`、`grep_files` 与 Git 只读探索，不会阻塞普通 Plan Mode。
- 待完成：仓库级后台索引生命周期、将 CodeGraph 返回的调用链/关联测试自动写入审批卡证据区，以及 Todo 的验收证据和计划偏离后二次审批。
